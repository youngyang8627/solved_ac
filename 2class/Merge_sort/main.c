#include <stdio.h>

void Merge_sort(int *arr[], int ini, int mid, int end)
{
	
}

void merge(int *arr[], int i, int n)
{
	int mid;

	mid = n / 2;
	
	
	

int main()
{
	int	n;
	int	i;
	int	a[10000];

	i = 0;
	scanf("%d",&n);
	while (i < n)
	{
		scanf("%d", a[i]);
		i++;
	}
	merge(a, 0, n);
}
