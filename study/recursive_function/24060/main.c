#include <stdio.h>

int cnt=0;
int hold;
int a[500001];

void merge(int in, int fi,int k){
	if(n>1){


int main()
{
	int n,k;//n = number for how many ints, k = save when k th
	scanf("%d %d",&n,&k);
	int i;
	for(i=0;i<n;i++){
		scanf("%d",&a[i]);
	}
	printf("%d",merge(0,n,k));
	return 0;
}
	
